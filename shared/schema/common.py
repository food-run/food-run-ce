"""
TL;DR
Starter surface for reusable common shape definitions.

Runtime role
- Hold shared shape vocabulary that is not owned by one app runtime.

Later extension points
- Add shared primitives reused across contract, domain, worker, and agent surfaces.
"""
