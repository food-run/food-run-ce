{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": ".",
  "x_tldr": "Starter configuration surface for the Food Run web application boundary.",
  "x_runtime_role": "Keep the web workspace rooted in apps/web without implying later runtime maturity.",
  "x_later_extension_points": [
    "Wire full Angular workspace settings here in later deliverables.",
    "Keep app-specific configuration inside the web boundary."
  ],
  "projects": {
    "web": {
      "projectType": "application",
      "root": ".",
      "sourceRoot": "src"
    }
  }
}
